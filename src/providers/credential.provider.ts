import mongoClient from "@/libs/mongo.client"
import { comparePasswords } from "@/libs/password"
import Credentials from "next-auth/providers/credentials"

// export default Credentials( ) as Credential
